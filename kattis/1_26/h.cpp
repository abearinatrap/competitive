#include <iostream>
#include <vector>
#include <map>
#include <stack>
#include <queue>
#include <set>
#include <algorithm>
#include <cassert>
using namespace std;

void solve(){
    int N;
    cin >> N;
    vector<int> nums(N);
    for(int i=0;i<N;++i){
        cin >> nums[i];
    }
    vector<int> times(N);
    for(int i=0;i<N;++i){
        int n,sum=0;
        cin >> n;
        for(int i=0;i<n;++i){
            
        }
    }
}

int main(){
    solve();
    return 0;
}
